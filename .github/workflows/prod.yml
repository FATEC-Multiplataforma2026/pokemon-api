name: Deploy Lambda AWS

on:
  push:
    branches:
      - main

jobs:
  build-deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Replace environment variables in template
        run: |
          sanitize() {
            printf '%s' "$1" | tr -d '\n' | sed 's/[\/&|]/\\&/g'
          }
          sed -i "s|!Ref appName|$(sanitize "${{ secrets.POKEMON_API_NAME }}")|g" template.yaml
          sed -i "s|!Ref appRegion|$(sanitize "${{ secrets.AWS_CLOUD_REGION_PROD }}")|g" template.yaml
          sed -i "s|!Ref appAccessKey|$(sanitize "${{ secrets.AWS_CLOUD_ACCESS_KEY }}")|g" template.yaml
          sed -i "s|!Ref appSecretKey|$(sanitize "${{ secrets.AWS_CLOUD_SECRET_KEY }}")|g" template.yaml
          sed -i "s|!Ref appDynamoTableName|$(sanitize "${{ secrets.POKEMON_API_DYNAMO_TABLE_NAME }}")|g" template.yaml
          sed -i "s|!Ref appDynamoCapturePokemonTable|$(sanitize "${{ secrets.POKEMON_DYNAMO_CAPTURE_POKEMON_TABLE }}")|g" template.yaml
          sed -i "s|!Ref appDynamoUsersTable|$(sanitize "${{ secrets.POKEMON_DYNAMO_USERS_TABLE }}")|g" template.yaml

      - name: Create env file
        run: |
          cat <<EOF > .env
          POKEMON_API_NAME=${{ secrets.POKEMON_API_NAME }}
          POKEMON_API_DYNAMO_REGION=${{ secrets.AWS_CLOUD_REGION_PROD }}
          POKEMON_API_ACCESS_KEY=${{ secrets.AWS_CLOUD_ACCESS_KEY }}
          POKEMON_API_SECRET_KEY=${{ secrets.AWS_CLOUD_SECRET_KEY }}
          POKEMON_API_DYNAMO_TABLE_NAME=${{ secrets.POKEMON_API_DYNAMO_TABLE_NAME }}
          POKEMON_DYNAMO_CAPTURE_POKEMON_TABLE=${{ secrets.POKEMON_DYNAMO_CAPTURE_POKEMON_TABLE }}
          POKEMON_DYNAMO_USERS_TABLE=${{ secrets.POKEMON_DYNAMO_USERS_TABLE }}
          POKEAPI_URL=${{ secrets.POKEAPI_URL }}
          EOF

      - name: Setup AWS SAM
        uses: aws-actions/setup-sam@v1

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
          aws-access-key-id: ${{ secrets.AWS_CLOUD_ACCESS_KEY }}
          aws-secret-access-key: ${{ secrets.AWS_CLOUD_SECRET_KEY }}
          aws-region: ${{ secrets.AWS_CLOUD_REGION_PROD }}

      - name: Generate Maven Wrapper
        run: mvn -N io.takari:maven:wrapper

      - name: Build GraalVM Docker image
        run: docker build -t al2023-graalvm21:native-web .

      - name: Build native image inside Docker
        run: |
          docker run --rm \
            --env-file .env \
            -v ${{ github.workspace }}:${{ github.workspace }} \
            -w ${{ github.workspace }} \
            -v ~/.m2:/root/.m2 \
            al2023-graalvm21:native-web \
            ./mvnw clean -Pnative package -DskipTests

      - name: List target folder
        run: ls -lah target

      - name: SAM Deploy
        run: |
          sam deploy \
            --no-confirm-changeset \
            --template-file template.yaml \
            --config-file samconfig.toml \
            --config-env prod \
            --no-fail-on-empty-changeset \
            --stack-name pokemon-api \
            --resolve-s3 \
            --capabilities CAPABILITY_IAM \
            --region ${{ secrets.AWS_CLOUD_REGION_PROD }}